'use strict';

describe('myApp.dsr module', function() {

  beforeEach(module('myApp.dsr'));

  describe('dsr controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dsrCtrl = $controller('DSRCtrl');
      expect(dsrCtrl).toBeDefined();
    }));

  });
});